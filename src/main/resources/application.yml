spring:
  datasource:
    url: jdbc:mysql://localhost:3306/freelearn?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    properties:
      hibernate:
        show_sql: false
        format_sql: true

logging:
  level:
    org:
      hibernate:
        sql: debug