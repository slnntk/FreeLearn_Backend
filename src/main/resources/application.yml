spring:
  datasource:
    url: jdbc:mysql://localhost:3306/freelearn3?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: validate
  sql:
    init:
      mode: embedded
      data-locations: classpath:initialize_database.sql

logging:
  level:
    org:
      hibernate:
        sql: debug
